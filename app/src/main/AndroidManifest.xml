<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.czy.yq_wanandroid">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:name=".base.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:configChanges="keyboard|keyboardHidden|screenSize|fontScale|density|orientation"
        android:theme="@style/Theme.Yq_wanAndroid">
        <activity
            android:name=".business.MainActivity"
            android:configChanges="keyboard|keyboardHidden|screenSize|fontScale|density|orientation">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".business.WebViewActivity"
            android:configChanges="keyboard|keyboardHidden|screenSize|fontScale|density|orientation" >
        </activity>
        <activity android:name=".business.projects.ProjectsListActivity" />
        <activity android:name=".business.login.LoginActivity" />
    </application>

</manifest>